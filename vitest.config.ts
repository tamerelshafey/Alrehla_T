import { defineConfig } from 'vitest/config';
import path from 'node:path';

/**
 * إعداد الاختبارات.
 *
 * الاختصار `@/` معرَّف هنا بالإيد بدل حزمة تقرأه من tsconfig — الحزمة دي
 * ESM والمشروع CJS، والتوفيق بينهم بيضيف تعقيدًا أكتر من اللي بيوفره.
 * لو الاختصار اتغيّر في tsconfig.json يومًا، لازم يتغيّر هنا كمان.
 *
 * الاختبارات جنب الكود في `src/**\/*.test.ts` عن قصد: الاختبار البعيد
 * عن الملف اللي بيختبره بيتنسى.
 *
 * ⚠️ `TZ=UTC` مهمة جدًا: خادم Vercel بيشتغل UTC، وأي جهاز تطوير في مصر
 * بيشتغل بتوقيت القاهرة. اختبار مواعيد بيعدّي على الجهاز المصري ويفشل
 * في الإنتاج — وده اللي خلّى عطل الجلسات (18:00 بتتسجّل UTC والمستخدم
 * يشوفها 21:00) يعدّي من غير ما حد ياخد باله. الاختبارات بتشتغل بنفس
 * توقيت الإنتاج دلوقتي.
 */
export default defineConfig({
  resolve: {
    alias: {
      '@': path.resolve(__dirname, 'src'),

      // `server-only` حزمة علامة: ملفها الافتراضي **بيرمي** خطأ عند
      // الاستيراد، عشان تمنع استيراد كود الخادم في الواجهة. Next بيقرا
      // نسخة فاضية منها في بيئة الخادم عن طريق شرط `react-server` —
      // و vitest مش بيعرف الشرط ده، فبيقرا الملف اللي بيرمي.
      //
      // من غير السطر ده، أي اختبار على `lib/auth-guard.ts` (وهي أول
      // سطر فيها `import 'server-only'`) بيفشل قبل ما يبدأ.
      'server-only': path.resolve(__dirname, 'node_modules/server-only/empty.js'),
    },
  },
  test: {
    environment: 'node',
    env: { TZ: 'UTC' },
    include: ['src/**/*.test.ts', 'src/**/*.test.tsx'],
  },
});
