# الرحلة (Alrehla) — تعليمات لأي أداة ذكاء اصطناعي تعمل على هذا المشروع

> **اقرأ هذا الملف قبل أي تعديل.** هو خلاصة أخطاء حقيقية وقعت في هذا المشروع
> ووصلت للموقع المنشور. كل قاعدة هنا مكتوبة لأن مخالفتها كلّفت شيئًا.

---

## 1. ما هو المشروع

منصة عربية للأطفال واليافعين، قسمان:

- **إنها لك** — قصص مطبوعة ومخصصة باسم الطفل وصورته
- **بداية الرحلة** — تدريب على الكتابة الإبداعية: باقات وجلسات مع مدربين،
  وخدمات إبداعية مستقلة (مراجعة نص، نشر قصة، استشارة تربوية)

**التقنيات:** Next.js 15 (App Router) · React 19 · TypeScript strict ·
Tailwind 4 · Supabase (Auth + Postgres + RLS) · Vercel · bun

---

## 2. القواعد التي لا تُخالَف

### ٢.١ لا تخترع بيانات. أبدًا.

هذا هو الخطأ الأكثر تكلفة في تاريخ المشروع. أمثلة حقيقية كانت معروضة على
الموقع لعملاء حقيقيين:

| ما كان مكتوبًا في الكود | الواقع |
|---|---|
| `01234567890` كرقم محفظة للتحويل | placeholder — عملاء حوّلوا لرقم غير موجود |
| `EG90000100000000000000000000` كـ IBAN للمستفيد | حساب مخترع، والإدارة تؤكد التحويل عليه |
| `4.9/5` تقييم كل مدرب | لا يوجد تقييم واحد في قاعدة البيانات |
| `+120 جلسة استشارية` | رقم مخترع |
| `أهلاً بك، طارق!` | اسم ثابت لكل عميل |
| فقرة نبذة كاملة تحت نبذة كل مدرب | نص واحد لكل المدربين |
| `price + 5000` كسعر "قبل الخصم" | خصم وهمي على كل منتج |
| ضريبة 5% تُضاف لفاتورة العميل | لا توجد ضريبة مُعرَّفة في المنصة |

**القاعدة:** إذا لم تكن البيانات في قاعدة البيانات، اعرض حالة فارغة صادقة
(«لا توجد تقييمات بعد»، «مدرب جديد»، «غير مسجّل») — لا رقمًا ولا اسمًا ولا
نصًا من عندك. الصفر ليس حلًا أيضًا: `0/5` يُقرأ كتقييم سيئ.

### ٢.٢ لا زر يكذب

زر مكتوب عليه «حفظ» ولا يحفظ أسوأ من عدم وجوده. كان في المشروع **أكثر من
عشرين** زرًا كهذا: نموذج الانضمام، تذكرة الدعم، تقرير الجلسة، قبول طلب
الانضمام، الرد على تذكرة، حفظ المقال، تحديث الشحن…

**القاعدة:** إما أن يعمل الزر، أو يكون `disabled` مع نص يقول إنه غير متاح.
ولا تكتب رسالة نجاح لعملية لم تحدث — كانت هناك شاشة تقول «تم حفظ الحضور
والتقرير، وتم إرسال نسخة للإدارة وللطالب» وهي لا تحفظ شيئًا.

### ٢.٣ لا `as any` ولا `(supabase as any)`

أضافت أداة سابقة **31** تحويلًا من هذا النوع في جلسة واحدة، فتعطّل فحص
الأنواع على كل استعلام في المشروع. إذا اشتكى TypeScript من استعلام، فالسبب
غالبًا أن `src/types/supabase.ts` قديم — **جدّده، لا تُسكِته**.

### ٢.٤ ملف الأنواع يتبع قاعدة البيانات، لا العكس

`src/types/supabase.ts` مولَّد من قاعدة البيانات الحقيقية. كان قديمًا مرة
فسمح بكتابة كود على أعمدة غير موجودة (`child_profiles.name`, `.age`)،
وشُحن معطلًا للإنتاج وظل كذلك شهورًا. أي تعديل على قاعدة البيانات يستوجب
تحديث هذا الملف في نفس الدفعة.

### ٢.٥ الحارس في قاعدة البيانات، لا في الكود

الفحص في الكود لرسالة خطأ واضحة. الحارس الحقيقي هو Row Level Security.

أمثلة مطبَّقة:
- تقييم لا يُكتب إلا من صاحب طلب **مكتمل**، مرة واحدة — بشرط في RLS وفهرس فريد
- المدرب لا يستطيع اعتماد سعر نفسه — بمُشغِّل (trigger)، لأن RLS تتحكم في
  الصف كله لا في عمود بعينه
- مبلغ الطلب يُحسب في السيرفر من العرض المعتمد، ولا يُؤخذ من المتصفح أبدًا

### ٢.٦ قبل أي ملف صلاحيات: اقرأ الموجود

```sql
SELECT policyname, cmd, qual, with_check FROM pg_policies
WHERE schemaname='public' AND tablename='<الجدول>';
```

أُضيفت قواعد مكررة على ثلاثة جداول لأن هذه الخطوة أُهملت.

### ٢.٧ البيانات التجريبية للتطوير المحلي فقط

`src/data/fixtures/**` لا يصل للموقع المنشور. كل رجوع إليها محروس بـ
`process.env.NODE_ENV === 'development'`. أربع دوال قراءة خالفت هذا فكانت
تعرض منتجًا بسعره ومدربًا بهويته من البيانات الوهمية عند فشل أي استعلام.

### ٢.٨ لا تعتمد على `git log` في بيئتك

الأدوات التي تعمل على نسخة مؤقتة من المستودع ترى `git log` الخاص بها، لا
ما وصل GitHub فعلًا. **المصدر الوحيد للحقيقة هو المستودع على GitHub.**

---

## 3. تنظيم الكود

```
src/
  data/domains/   ← القراءة فقط (استعلامات Supabase)
  data/fixtures/  ← بيانات تجريبية للتطوير المحلي فقط
  actions/        ← الكتابة فقط ('use server')
  app/            ← الصفحات والمسارات
  components/     ← مكوّنات مشتركة
  lib/            ← أدوات (supabase, utils, cloudinary, notifications, audit)
  types/          ← index.ts (أنواع التطبيق) + supabase.ts (مولَّد)
```

- صفحة لا تستدعي Supabase مباشرة — تمر بـ `data/domains` أو `actions`
- كل عملية كتابة إدارية تُسجَّل في `audit_logs` عبر `logAuditAction`
- الإشعارات عبر `notifyUser` — وهي best-effort: فشلها لا يُفشل العملية

---

## 4. الحقائق التجارية (لا تخترع بديلًا لها)

| | |
|---|---|
| الدفع | **تحويل فقط** (محفظة/إنستاباي). لا دفع بالبطاقة حتى ربط بوابة رسمية — ولا تطلب بيانات بطاقة على الصفحة إطلاقًا |
| رقم المحفظة | من `site_settings.paymentWalletNumber`، ويُعدَّل من لوحة الإدارة |
| سعر العميل للجلسات والخدمات | `حصيلة المدرب × المضاعِف + الرسوم الثابتة` من `pricing_formula_settings` |
| الشحن | من جدول `shipping_rates` حسب المنطقة — ليس رقمًا ثابتًا |
| مدة التنفيذ | إلكتروني 3–5 أيام عمل · مطبوع 7–10 أيام عمل، بخلاف الشحن |
| نسبة الناشر | تتبع معادلة التسعير، وليست 70% ثابتة |
| بريد الدعم | `Alrehla@tech-bokra.com` |
| الجلسات | Google Meet أو Dooly — الرابط في `sessions.meeting_url` |
| رفع الصور | Cloudinary، preset غير موقّع. **لا تضع API Secret في الكود** |

---

## 5. قبل أن تقول «خلصت»

1. `npm install`
2. `npx tsc --noEmit` — بلا أخطاء
3. `npm run build` — ينجح
4. لا `as any` جديد: `grep -rn "as any" src | wc -l` لا يزيد
5. لا نص مخترع جديد: أي رقم أو اسم كتبته، من أين مصدره؟
6. أي زر أضفته — هل هو موصول فعلًا؟
